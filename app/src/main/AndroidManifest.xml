<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.progetto_mobile"
    android:screenOrientation="portrait"
    tools:ignore="LockedOrientationActivity">
    <!-- blocca rotazione per corretta visualizzazione -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launcher"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MyRides"></activity>
        <activity android:name=".ShowRides" />
        <activity android:name=".InsertRide" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".LicenceActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>